var pkg = require('../../package.json');

module.exports = {
    siteName: 'Design Prototype',
    appDescription: pkg.description || 'Generic description.',
    appName: pkg.name,
    appVersion: pkg.version,
    appAuthor: pkg.author,
};
